%section.content-header
	%h1 
		Adding new Images to #{@establishment.name}

%section.content
	= render 'form'

/ = render 'admin/establishments/picform'

= link_to 'Back', admin_establishment_pictures_path, class: "label label-primary"
